/**
 * Person compile — backward-compatibility re-export.
 * Use compile-entity.ts for all entity types.
 */
export { compileEntity as compilePerson } from './compile-entity.ts';
export type { EntityState } from './compile-entity.ts';
